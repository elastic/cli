# Copyright Elasticsearch B.V. and contributors
# SPDX-License-Identifier: Apache-2.0
#
# Kibana elastic-agent-actions API: bulk remove OpAMP collectors from agents.
---
requires:
  serverless: true
  stack: true
---
setup:
  - do:
      elastic-agents.get_fleet_agents: {}
  - set: { "items.0.id": agent_id }
---
"elastic-agent-actions post fleet agents bulk remove collectors":
  - do:
      elastic-agent-actions.post_fleet_agents_bulk_remove_collectors:
        body:
          agents:
            - $agent_id
  - is_true: actionId
