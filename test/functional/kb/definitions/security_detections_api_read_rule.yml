# Copyright Elasticsearch B.V. and contributors
# SPDX-License-Identifier: Apache-2.0
#
# Kibana security-detections-api: retrieve a detection rule by ID.
---
requires:
  serverless: true
  stack: true
---
setup:
  - do:
      security-detections-api.create_rule:
        body:
          type: query
          name: cli-ft-sec-det-read-rule
          description: Created by functional test
          risk_score: 21
          severity: low
          query: "*"
  - set: {id: rule_id}
---
teardown:
  - do:
      security-detections-api.delete_rule:
        id: $rule_id
        ignore: [404]
---
"security-detections-api read rule":
  - do:
      security-detections-api.read_rule:
        id: $rule_id
  - is_true: id
  - match: {name: cli-ft-sec-det-read-rule}
