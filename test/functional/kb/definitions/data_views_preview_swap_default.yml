# Copyright Elasticsearch B.V. and contributors
# SPDX-License-Identifier: Apache-2.0
#
# Kibana data-views API: preview swap saved object references from one data view to another.
---
requires:
  serverless: true
  stack: true
---
setup:
  - do:
      data-views.create_data_view_default:
        body:
          data_view: { title: "cli-ft-dv-preview-swap-from-*", name: cli-ft-dv-preview-swap-from }
  - set: { data_view.id: from_id }
  - do:
      data-views.create_data_view_default:
        body:
          data_view: { title: "cli-ft-dv-preview-swap-to-*", name: cli-ft-dv-preview-swap-to }
  - set: { data_view.id: to_id }
---
teardown:
  - do:
      data-views.delete_data_view_default:
        viewId: $from_id
        ignore: [404]
  - do:
      data-views.delete_data_view_default:
        viewId: $to_id
        ignore: [404]
---
"data views preview swap default":
  - do:
      data-views.preview_swap_data_views_default:
        body:
          fromId: $from_id
          toId: $to_id
  - length: { preview: 0 }
