# Copyright Elasticsearch B.V. and contributors
# SPDX-License-Identifier: Apache-2.0
#
# Kibana fleet-cloud-connectors API: update a cloud connector.
---
requires:
  serverless: true
---
setup:
  - do:
      fleet-cloud-connectors.post_fleet_cloud_connectors:
        body:
          name: cli-ft-cloud-connector-put
          cloudProvider: aws
          vars:
            role_arn:
              type: text
              value: arn:aws:iam::123456789012:role/cli-ft
            external_id:
              type: text
              value: cli-ft-external-id-1234567890
  - set: { "item.id": connector_id }
---
teardown:
  - do:
      fleet-cloud-connectors.delete_fleet_cloud_connectors_cloudconnectorid:
        cloud_connector_id: $connector_id
        ignore: [404]
---
"fleet-cloud-connectors put fleet cloud connectors cloudconnectorid":
  - do:
      fleet-cloud-connectors.put_fleet_cloud_connectors_cloudconnectorid:
        cloud_connector_id: $connector_id
        body:
          name: cli-ft-cloud-connector-put-updated
  - is_true: item.id
  - is_true: item.name
