# Copyright Elasticsearch B.V. and contributors
# SPDX-License-Identifier: Apache-2.0
# Cloud control plane API: Get Deployment upgrade assistant status (GET /api/v1/deployments/{deployment_id}/upgrade_assistant/status)
# Id for {deployment_id} is derived from deployments.list-deployments.
# TODO: verify the response path in the `set` step against a live API.
---
"deployments get-deployment-upgrade-assistant-status":
  - do:
      deployments.list-deployments: {}
  - set: {deployments.0.id: deployment_id}
  - do:
      deployments.get-deployment-upgrade-assistant-status:
        deployment_id: $deployment_id
