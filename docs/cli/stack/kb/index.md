## Description

The `elastic stack kb` command group exposes {{kib}} REST APIs as CLI commands. Use the generated command reference to find supported operations and their input options.

These commands are also available using the `elastic kb` or `elastic kibana` shortcuts.
