## Description

The `elastic stack es` command group exposes [{{es}} REST APIs](elasticsearch://reference/elasticsearch/rest-apis/index.md) as CLI commands. Use the generated command reference to find supported operations and their input options.

These commands are also available using the `elastic es` or `elastic elasticsearch` shortcuts.
